# variant-calling-pipeline
End‑to‑end DNA variant calling pipeline that processes raw FASTQ reads into a final VCF using FastQC, BWA, Samtools, FreeBayes, and bcftools. Includes quality control, alignment, variant calling, filtering, and reproducible workflow automation with Snakemake.
